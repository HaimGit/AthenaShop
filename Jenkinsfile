pipeline {
    agent any 
    tools { nodejs "nodejs" }

    stages {
        stage('npm install') {
            steps {
                sh "npm install"
            }
        }

        stage('Build') {

        }

        stage('Run e2e tests') {

        }

        stage('Deploy') {

        }
    }
}