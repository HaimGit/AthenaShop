pipeline {
    agent any 
    tools { nodejs "nodejs" }

    stages {
        stage('npm install') {
            steps {
                sh "npm install"
            }
        }

        stage('Build') {
            steps {

            }
        }

        stage('Run e2e tests') {
            steps {

            }
        }

        stage('Deploy') {
            steps {
                
            }
        }
    }
}