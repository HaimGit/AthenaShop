pipeline {
    agent any 
    tools { nodejs "nodejs" }

    stages {
        stage('npm install') {
            sh "npm install"
        }

        stage('Build') {

        }

        stage('Run e2e tests') {

        }

        stage('Deploy') {

        }
    }
}